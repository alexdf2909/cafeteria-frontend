export type Item = {
    id: number;
    name: string;
    category: string;
    unit: string;
    stockTotal: string;
    stockMinimum: string;
    nearestExpiration: string;
    location: string;
    level: string;
}